import 'package:flutter/material.dart';

abstract class AppColors{
  static const primary = Color(0xff805AF5);
  static const background = Color(0xff0E0C15);
  static const light = Color(0xffffffff);
  static const dark = Color(0xff21242D);
  static const darkest = Color(0xff16181E);
}